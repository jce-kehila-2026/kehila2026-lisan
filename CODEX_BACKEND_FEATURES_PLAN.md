# خطة عمل Codex (السباق المتقدم) - ميزات الباك إند (Backend Features)

**الدور (Persona):** مهندس باك إند خبير بـ Node.js و Express.js و Firebase.
**القاعدة الذهبية:** يمنع منعاً باتاً التعديل على أي ملف يخص واجهة المستخدم (Frontend). العمل محصور في `backend/`.

## مرحلة إدارة المحادثات المتقدمة (Advanced Conversation Management)

### Task 1: توليد عناوين المحادثات تلقائياً (Auto-Titling)
- **الهدف:** إعطاء كل محادثة جديدة عنواناً ذكياً بدلاً من عنوان ثابت.
- **التنفيذ:** في `chatPersistenceService.js`، عند إنشاء محادثة جديدة، قم بأخذ أول 10 كلمات من رسالة المستخدم الأولى، واجعلها هي قيمة حقل `title` في مستند المحادثة.

### Task 2: منع تكرار الرسائل (Idempotency)
- **الهدف:** منع حفظ نفس الرسالة مرتين إذا قام المستخدم بعمل Retry للشبكة.
- **التنفيذ:** في `chatController.js`، استخدم `clientMessageId` القادم من الطلب. قبل حفظ رسالة جديدة، تحقق مما إذا كانت هناك رسالة بنفس الـ `clientMessageId` موجودة مسبقاً في نفس المحادثة. إذا كانت موجودة، أرجع الرد السابق بدلاً من إنشاء رد جديد.

### Task 3: أرشفة المحادثات (Conversation Archiving)
- **الهدف:** السماح للمستخدم بإخفاء المحادثات القديمة دون حذفها.
- **التنفيذ:** أضف مسار `POST /api/chat/conversations/:id/archive`. سيقوم الـ Controller بوضع علامة `isArchived: true` على المحادثة. قم بتعديل مسار `GET /api/chat/conversations` ليستثني المحادثات المؤرشفة افتراضياً.

## مرحلة تخصيص تجربة المستخدم (User Personalization)

### Task 4: حفظ تفضيلات المستخدم (User Preferences)
- **الهدف:** جعل البوت يتذكر إعدادات المستخدم المفضلة.
- **التنفيذ:** أضف مسار `POST /api/chat/preferences` يحفظ إعدادات مثل `defaultIncludeArabic: true` في مستند المستخدم الرئيسي في Firestore. عند بدء محادثة جديدة، اقرأ هذه التفضيلات وطبقها.

### Task 5: المزامنة التلقائية لمستوى الطالب (Level Sync)
- **الهدف:** التأكد من أن البوت يستخدم دائماً المستوى الصحيح للطالب.
- **التنفيذ:** في `chatController.js`، عند إنشاء محادثة جديدة، تجاهل حقل `level` القادم من الطلب واقرأ المستوى الحقيقي للطالب من `req.user.level` لضمان الدقة.

## مرحلة التحليلات والمراقبة (Analytics & Monitoring)

### Task 6: دمج نظام تسجيل أخطاء منظم (Structured Error Logging)
- **الهدف:** تسهيل تتبع الأخطاء في بيئة الإنتاج.
- **التنفيذ:** استبدل كل `console.error` في مسارات الشات بمكتبة تسجيل منظمة مثل `pino` أو `winston`. قم بتسجيل الأخطاء كـ JSON مع تفاصيل مفيدة مثل `userId` و `conversationId`.

### Task 7: بناء واجهة تحليلات أساسية للمشرف (Admin Analytics)
- **الهدف:** توفير رؤية حول استخدام الميزة.
- **التنفيذ:** أنشئ مساراً جديداً محمياً للمشرفين فقط `GET /api/admin/chat/stats`. سيقوم هذا المسار بإرجاع إحصائيات مثل: عدد الرسائل اليوم، عدد المحادثات الجديدة، وعدد المستخدمين النشطين.

## مرحلة تحسين الأداء والجودة (Performance & Quality)

### Task 8: تعريف فهارس قاعدة البيانات (Firestore Indexing)
- **الهدف:** ضمان سرعة استعلامات Firestore مع نمو البيانات.
- **التنفيذ:** أنشئ ملف `firestore.indexes.json` في مجلد `backend/`. قم بتحليل جميع الاستعلامات المستخدمة في `chatPersistenceService` وحدد الفهارس المركبة (Composite Indexes) المطلوبة. وثّق كيفية نشر هذه الفهارس في الـ `README`.

### Task 9: اختبارات أداء لقاعدة البيانات (Persistence Load Testing)
- **الهدف:** التأكد من أن عملية الحفظ لا تبطئ مع كثرة الرسائل.
- **التنفيذ:** باستخدام `supertest` أو أداة أخرى، اكتب اختباراً يقوم بإنشاء محادثة ثم يضيف إليها 100 رسالة بشكل متتابع، وقم بقياس متوسط زمن الاستجابة للتأكد من عدم وجود تدهور كبير في الأداء.

### Task 10: تنظيف وتوحيد المسارات القديمة (Route Refactoring)
- **الهدف:** إزالة أي كود قديم أو مسارات مكررة لتحسين صيانة الكود.
- **التنفيذ:** قم بمراجعة `backend/src/server.js` وجميع ملفات الـ routes. إذا كان هناك أي مسار قديم مثل `/api/chats` لا يزال موجوداً، قم بوضع علامة "مهمل" (`@deprecated`) عليه مع تحذير في الكونسول، وخطط لإزالته بالكامل.