# خطة عمل كلود (السباق المتقدم) - تطوير الذكاء الاصطناعي والباك إند (AI/Backend Advanced)

**الدور (Persona):** مهندس ذكاء اصطناعي وباك إند خبير (Senior AI & Backend Architect).
**القاعدة الذهبية:** يمنع منعاً باتاً التعديل على أي ملف يخص واجهة المستخدم (Frontend). العمل محصور في `ai-service` و `backend`.

## مرحلة التقييم والذاكرة (Assessment & Memory)

### Task 1: تقييم النطق (Pronunciation Assessment)
- **الهدف:** إعطاء الطالب تقييماً لمدى صحة نطقه للعبرية.
- **التنفيذ:** دمج ميزة `Pronunciation Assessment` الخاصة بـ Azure Speech Services مع مسار STT الحالي. إرجاع درجة (Score) من 0 إلى 100 في استجابة `VoiceChatResponse` دون تعطيل سرعة الاستجابة.

### Task 2: ذاكرة السياق متعددة الجولات (Multi-turn Context Memory)
- **الهدف:** جعل البوت يتذكر آخر 3-5 رسائل ليكون الحوار طبيعياً.
- **التنفيذ:** استخدام `Redis` أو آلية Caching سريعة في FastAPI لحفظ سياق المحادثة (Chat History) لكل `session_id`. تمرير هذا السياق إلى محرك `generate_chat_response` ليفهم النموذج تسلسل الحوار.

### Task 3: التتبع الذكي للمفردات (Vocabulary Mastery Tracking)
- **الهدف:** معرفة الكلمات التي أتقنها الطالب وتلك التي يخطئ بها.
- **التنفيذ:** إنشاء وحدة (Module) تقوم بتحليل `transcribedText` ومقارنته بقائمة مفردات A1 المسموحة، ثم إرسال تحديث للـ Backend (Express/Firestore) لزيادة رصيد الكلمات المتقنة للطالب.

## مرحلة تحسين جودة التعليم (Pedagogical Enhancements)

### Task 4: تحسين نبرة المعلم (TTS SSML Tuning)
- **الهدف:** جعل الصوت أكثر تفاعلية وتشجيعاً.
- **التنفيذ:** إذا تم الاعتماد لاحقاً على TTS من جهة السيرفر، أو عند بناء الـ Prompts، قم بتضمين علامات SSML لتحسين التوقفات (Pauses) وتعديل نبرة الصوت لتكون ودودة أكثر عند الإجابة الصحيحة.

### Task 5: التوجيه الديناميكي للقواعد (Dynamic Grammar Retrieval)
- **الهدف:** شرح الأخطاء النحوية فور وقوعها.
- **التنفيذ:** تحسين `chat_retrieval.py` ليقوم برصد الأخطاء النحوية المتكررة في نص الطالب (مثلاً استخدام مذكر بدل مؤنث)، وحقن القاعدة النحوية الصحيحة كـ System Prompt مخفي لتوجيه الـ LLM في طريقة التصحيح.

### Task 6: كوابح متكيفة (Adaptive Guardrails)
- **الهدف:** تعديل صرامة البوت بناءً على مستوى الطالب.
- **التنفيذ:** جعل `chat_guardrails.py` يقرأ متغير `level` (A1, A2). إذا كان A1، يمنع العبرية المعقدة بصرامة (ويفعل OUT_OF_SCOPE). إذا تقدم الطالب لـ A2، يسمح بمفردات أكثر ويكون أكثر تساهلاً.

## مرحلة الأداء والتقييم المستمر (Performance & Evals)

### Task 7: دعم المعالجة المجزأة (Chunked Processing / Streaming Prep)
- **الهدف:** تقليل وقت الانتظار (Latency) للمحادثات الطويلة.
- **التنفيذ:** إعادة هيكلة `chat_engine.py` ليدعم إرجاع الردود كـ Stream (Tokens) من LLM، لتجهيز البنية التحتية مستقبلاً لبث الصوت أو النص أجزاءً أجزاء.

### Task 8: مقيّم الردود الآلي (LLM-as-a-Judge Eval)
- **الهدف:** التحقق من الجودة التعليمية لردود البوت بشكل آلي.
- **التنفيذ:** كتابة سكريبت تقييم جديد في مجلد `evals` يستخدم نموذجاً أقوى (مثل GPT-4o) لقراءة الـ `answerHe` الناتج وتقييمه من 1 إلى 5 بناءً على: بساطة اللغة، ملاءمة مستوى A1، وعدم وجود هلوسة.

### Task 9: واجهات تحليل الأداء (Analytics Endpoints)
- **الهدف:** توفير بيانات لداشبورد الإدارة.
- **التنفيذ:** إنشاء راوت جديد `GET /api/ai/analytics` يجمع إحصائيات من الـ Cache حول معدلات الفشل (STT/TTS Fallbacks)، متوسط سرعة الاستجابة (Latency)، وأكثر أسباب الكوابح (Guardrails) تفعيلاً.

### Task 10: اختبارات التحمل (Stress & Load Testing)
- **الهدف:** التأكد من صمود الـ AI Service تحت الضغط.
- **التنفيذ:** استخدام أداة مثل `Locust` أو `Artillery` لكتابة سكريبت يرسل 50 طلب صوتي متزامن (Concurrent) إلى مسار `POST /api/ai/chat/voice`، والتأكد من تفعيل الـ Circuit Breakers بشكل صحيح دون انهيار السيرفر.