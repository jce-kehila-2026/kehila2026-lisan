# خطة عمل كوديكس/كيرسور - تثبيت واجهة المستخدم (UI Stabilization)

**الدور (Persona):** أنت مطور واجهات تفاعلية خبير بـ React و Tailwind CSS.
**المهمة:** تنظيف واجهة المستخدم من تعارضات الدمج، ربطها بشكل سليم مع الباك إند المستقر، وتوفير تجربة خالية من العيوب.

يرجى تنفيذ المهام التالية بالترتيب (Task by Task):

## Task 1: حل تعارضات الدمج في مكونات الواجهة (Resolve Merge Conflicts)
**الهدف:** جعل المشروع يعمل بدون أخطاء Compilation.
- ابحث عن أي `<<<<<<< HEAD` في مجلد `frontend/src/` (خاصة في `ChatComposer.jsx`, `ChatMessage.jsx`, أو `App.jsx`).
- قم بحل التعارضات مع الحفاظ على التصميم المتفق عليه وهيكل المكونات السليم.

## Task 2: توافق الـ API والـ Error Handling (Frontend-Backend Alignment)
**الهدف:** التأكد من أن الفرونت إند يستوعب ردود الباك إند بشكل صحيح.
- راجع `frontend/src/services/chat.js` وتأكد من الربط الصحيح مع نقطة نهاية `Express.js` (وليس الـ AI Service مباشرة).
- أضف معالجة شاملة للأخطاء بحيث إذا أرجع الباك إند رسالة خطأ (مثلاً `STT_FAILED`)، يتم عرض تنبيه لطيف للمستخدم داخل واجهة الشات.

## Task 3: تثبيت حالة تسجيل الصوت (Voice UI Stability)
**الهدف:** ضمان تجربة تسجيل صوتي لا تعلق ولا تنهار.
- راجع الـ Hook المسمى `useAudioRecorder`.
- تأكد من التعامل مع الحالات الاستثنائية: رفض المستخدم لصلاحية الميكروفون، انقطاع التسجيل، أو الضغط المزدوج السريع على زر المايك.
- تأكد من إخفاء/إظهار التأثيرات البصرية (Wave/Pulse) بشكل متزامن تماماً مع حالة الـ Recorder.

## Task 4: تحسينات الـ UX ودعم الـ RTL (UX Polish)
**الهدف:** جعل الواجهة تبدو احترافية وجاهزة للإنتاج.
- راجع تنسيقات Tailwind للتأكد من أن كل النصوص العربية والعبرية تُعرض بالاتجاه الصحيح (RTL/LTR Support).
- تأكد من تعطيل زر الإرسال أو زر المايكروفون (Disabled State) أثناء انتظار رد الذكاء الاصطناعي (Loading State).
- تأكد من أن الشاشة تقوم بالتمرير التلقائي (Auto-scroll) للأسفل عند وصول رسالة جديدة.

## Task 5: اختبار الدخان للواجهة (Frontend Smoke Testing)
**الهدف:** محاكاة مستخدم حقيقي لضمان الجاهزية 100%.
- قم بإجراء تدفق كامل يدوياً (أو عبر Cypress إن وجد):
  1. تسجيل الدخول.
  2. إرسال رسالة نصية واستقبال الرد.
  3. إرسال رسالة صوتية، رؤية الـ Loader، وسماع الرد الصوتي تلقائياً.
  4. تحديث الصفحة والتأكد من بقاء الرسائل في الـ History.

---
**النتيجة المتوقعة بنهاية الخطة:** UI مستقر، لا يعرض أخطاء بالكونسول، وتجربة المستخدم سلسة 100%.